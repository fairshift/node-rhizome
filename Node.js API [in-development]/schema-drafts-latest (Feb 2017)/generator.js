
// Input: Collections (via SimpleSchema -> JSON Schema) -> indexed SQL schema

	// 


// Generate SQL schema

	// Attach Rz.action fields to table

	// Caching table: ${collectionName}_rzcache


// Migrations

	// Change action definition -> generate migration schema recipe
	// 

// Migrations - SQL

	// Rename tables, rename fields, modify indexes
	// Drop tables, drop fields
	// Create tables, add fields

// Migrations - NoSql
	
	// Query for data (collection by collection) -> build JSON object
	// Validate data by schema
	// Export data & checksums

	// Traverse JSON objects by migration schema recipe
	// Validate data by replaced schema
	// Export checksums
	// Modify data, object by object, according to migration schema


// Generate indexes
	
	// 